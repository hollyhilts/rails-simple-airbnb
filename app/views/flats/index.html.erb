<div class="navbar">
  <h3>Simple Airbnb</h3>
  <button type="button" class="btn btn-outline-primary text-decoration-none">
    <%= link_to 'add flat', new_flat_path %>
  </button>
</div>

<div class="container">
  <h1>all our flats (<%= @flats.count %>)</h1>

    <div class="cards">
  <% @flats.each do |flat| %>
      <div class="flat_card">
        <img class="flat_card_img"src="https://source.unsplash.com/featured/?apartments" alt="a randomized room image">
        <h1><%= flat.name %></h1>
        <p><%= flat.address %></p>
        <div class="d-flex justify-content-between">
          <div class="d-flex">
            <i class="material-icons">people</i>
            <p class="mr-4"><%= flat.number_of_guests %></p>
          </div>
          <div class="d-flex">
            <i class="material-icons">attach_money</i>
            <p class="ml-4"><%= flat.price_per_night %></p>
          </div>
        </div>
        <p><%= link_to 'more info', flat_path(flat) %></p>
      </div>
  <% end %>
    </div>
</div>

name, as a string
address, as a string
description, as a text
price_per_night, as an integer
number_of_guests, as an integer
